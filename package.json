{
  "name": "fractured-earth-rts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild src/server/main.ts --bundle --platform=node --format=esm --packages=external --outfile=server.mjs",
    "start": "node server.mjs"
  },
  "dependencies": {
    "three": "^0.165.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/three": "^0.165.0",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
